# @package _global_

data:
  task_name: KB_SentiNeg
  max_length: 128

optim:
  epochs: 5
  batch_size: 16
  grad_acc: 1
  base_lr: 1e-02
  dropout_prob: 0.1
  warmup_ratio: 0.1

logging:
  log_steps: 50

